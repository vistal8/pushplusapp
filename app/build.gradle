plugins {
    id "com.android.application"
}

android {
    namespace "local.pushplus.history"
    compileSdk 36

    defaultConfig {
        applicationId "local.pushplus.history.app"
        minSdk 23
        targetSdk 36
        versionCode 2
        versionName "1.0"
    }
}
